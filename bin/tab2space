#!/bin/bash

[ "$1" = -- ] && shift
typeset -a converterCommand=(tr $'\t' ' ')
if [ $# -eq 0 ]; then
    exec "${converterCommand[@]}"
else
    set -o pipefail
    cat -- "$@" | "${converterCommand[@]}"
fi
